<%if @user%><h1><%if @user == current_user%>Hi <%=@user.name%>!</h1><%else%><h1><%=@user.name%>'s Page</h1><%end%>
<%end%>


<%if @user == current_user%>
<h3>Your Info:</h3><p><ul>
   <li>Username: <%=@user.username%></li>
    <li>Email: <%=@user.email%></li> 
    <%else%> 
    <li> Username: <%=@user.username%></li>
    <li> Email: <%=@user.email%></li>
    <%end%>
</ul></p>
<%# <%= button_to "Log Out", { action: "delete"}, method: :delete, data: { confirm: "Are you sure?" } %>
    <%# <form method="post" action="sessions" class="button_to">
      <input type="hidden" name="_method" value="delete" />
      <input data-confirm='Are you sure?' value="Log Out" type="submit" />
      <#input name="authenticity_token" type="hidden" value="10f2163b45388899ad4d5ae948988266befcb6c3d1b2451cf657a0c293d605a6"/>
    </form>" %>
    <%if @user.plants.present? && @user == current_user%>
    
    <h3>Your Plants:</h3> 
     <%@user.plants.each do |plant|%>
    <ul><li><%=link_to plant.nickname, plant_path(plant)%> the <%=plant.plant_type%>--<%=link_to "Edit #{plant.nickname}", edit_plant_path(plant)%> </li></ul><%end%>
    <%elsif @user.plants.present?%> <h3>Plants:</h3>
    <%@user.plants.each do |plant|%>
    <ul><li><%=link_to plant.nickname, plant_path(plant)%> the <%=plant.plant_type%></li></ul><%end%>
    <br><%else%> <h3>No Plants Yet</h3>
   <%end%>
   <%#if  current_user==@user%>
    <%= button_to "Log Out", logout_path, :method => :delete %>
    <%if !logged_in?%>
    <p>Please <%=link_to "Sign Up", signup_path%> or <%=link_to "Log in", login_path%></p><%end%>

    
